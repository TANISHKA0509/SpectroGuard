# SpectroGuard - Python dependencies
#
# NOTE: torch/torchvision are pinned to CPU-friendly versions so the project
# runs on any machine. On Windows/local installs prefer the CPU wheels:
#   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cpu
# The Dockerfile already installs them from the CPU index for smaller images.

torch==2.4.1
torchvision==0.19.1
timm==1.0.9

numpy==1.26.4
opencv-python-headless==4.10.0.84
pillow==10.4.0

fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19

huggingface_hub==0.27.0
requests==2.32.3
PyJWT==2.9.0
